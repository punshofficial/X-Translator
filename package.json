{
  "name": "x-translator",
  "version": "0.3.1",
  "private": true,
  "license": "MIT",
  "description": "Локальное Chrome/Edge-расширение для автоперевода постов X на русский.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check background.js && node --check content.js && node --check popup.js && node --check tweet-data.js && node --check x-data-bridge.js && npm test",
    "package": "bash scripts/package.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
