{
  "name": "x-translator",
  "version": "0.2.7",
  "private": true,
  "description": "Локальное Chrome/Edge-расширение для автоперевода постов X на русский.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check background.js && node --check content.js && node --check popup.js && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
